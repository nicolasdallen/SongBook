name: songbook
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0
  pdf: ^4.0.0
  musicxml: ^1.0.0
  midi_player: ^2.0.0
  flutter_tts: ^3.0.0  # For text-to-speech AI integration
  openai_api: ^1.0.0  # For AI integration with OpenAI API

flutter:
  uses-material-design: true
  
